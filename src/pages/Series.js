import React from 'react';
import MovieList from '../components/MovieList';

const Series = () => {
  return (
    <div>
      <h2>Series</h2>
      <MovieList />
    </div>
  );
};

export default Series;
