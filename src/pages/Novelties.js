import React from 'react';
import MovieList from '../components/MovieList';

const Novelties = () => {
  return (
    <div>
      <h2>Página de Novedades</h2>
      <MovieList />
    </div>
  );
};

export default Novelties;
